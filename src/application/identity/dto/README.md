# DTOs

Data Transfer Objects used by the Presentation layer.

DTOs define and validate the data exchanged through the HTTP API.

Responsibilities:
- Define request data
- Define response data when necessary
- Validate external input

DTOs should not contain business rules.